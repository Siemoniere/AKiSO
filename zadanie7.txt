#!/bin/bash

for file in *; do
    if [ -f "$file" ]; then
        small=$(echo "$file" | tr '[:upper:]' '[:lower:]')
        if [ "$file" != "$small" ]; then
            mv "$file" "$small"
        fi
    fi
done
